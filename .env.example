# Production secrets — configure these in the hosting environment, never commit real values.
FLW_PUBLIC_KEY=FLWPUBK-REPLACE_WITH_ROTATED_LIVE_PUBLIC_KEY
FLW_SECRET_KEY=FLWSECK-REPLACE_WITH_ROTATED_LIVE_SECRET_KEY
FLW_ENCRYPTION_KEY=REPLACE_WITH_ROTATED_ENCRYPTION_KEY
FLW_WEBHOOK_HASH=REPLACE_WITH_FLUTTERWAVE_WEBHOOK_HASH

# The application will also use values saved through the protected admin Settings page.
# Live subscriptions activate only after server-side Flutterwave verification.
